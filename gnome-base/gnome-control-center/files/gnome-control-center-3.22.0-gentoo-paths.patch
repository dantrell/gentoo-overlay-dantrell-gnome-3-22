diff '--exclude=.git' -urN a/panels/datetime/Makefile.am b/panels/datetime/Makefile.am
--- a/panels/datetime/Makefile.am	2016-09-20 04:49:16.000000000 -0400
+++ b/panels/datetime/Makefile.am	2016-09-28 00:06:38.848143847 -0400
@@ -23,6 +23,7 @@
 	$(DATETIME_PANEL_CFLAGS)			\
 	-DGNOMELOCALEDIR="\"$(datadir)/locale\""	\
 	-DGNOMECC_DATA_DIR="\"$(pkgdatadir)\""		\
+	-DREALDATADIR="\"$(datadir)\""                  \
 	$(NULL)
 
 # test-timezone is still too noisy
diff '--exclude=.git' -urN a/panels/datetime/test-endianess.c b/panels/datetime/test-endianess.c
--- a/panels/datetime/test-endianess.c	2016-09-20 04:49:16.000000000 -0400
+++ b/panels/datetime/test-endianess.c	2016-09-28 00:06:38.848143847 -0400
@@ -26,11 +26,11 @@
 	GDir *dir;
 	const char *name;
 
-	dir = g_dir_open ("/usr/share/i18n/locales/", 0, NULL);
+	dir = g_dir_open (REALDATADIR "/i18n/locales/", 0, NULL);
 	if (dir == NULL) {
 		/* Try with /usr/share/locale/
 		 * https://bugzilla.gnome.org/show_bug.cgi?id=646780 */
-		dir = g_dir_open ("/usr/share/locale/", 0, NULL);
+		dir = g_dir_open (REALDATADIR "/locale/", 0, NULL);
 		if (dir == NULL) {
 			g_assert_not_reached ();
 		}
diff '--exclude=.git' -urN a/panels/datetime/test-timezone.c b/panels/datetime/test-timezone.c
--- a/panels/datetime/test-timezone.c	2016-09-20 04:49:16.000000000 -0400
+++ b/panels/datetime/test-timezone.c	2016-09-28 00:06:38.848143847 -0400
@@ -2,7 +2,7 @@
 #include <gtk/gtk.h>
 #include "cc-timezone-map.h"
 
-#define TZ_DIR "/usr/share/zoneinfo/"
+#define TZ_DIR REALDATADIR "/zoneinfo/"
 
 static GList *
 get_timezone_list (GList *tzs,
diff '--exclude=.git' -urN a/panels/datetime/tz.h b/panels/datetime/tz.h
--- a/panels/datetime/tz.h	2015-03-17 07:25:21.000000000 -0400
+++ b/panels/datetime/tz.h	2016-09-28 00:06:38.848143847 -0400
@@ -28,9 +28,9 @@
 #include <glib.h>
 
 #ifndef __sun
-#  define TZ_DATA_FILE "/usr/share/zoneinfo/zone.tab"
+#  define TZ_DATA_FILE REALDATADIR "/zoneinfo/zone.tab"
 #else
-#  define TZ_DATA_FILE "/usr/share/lib/zoneinfo/tab/zone_sun.tab"
+#  define TZ_DATA_FILE REALDATADIR "/lib/zoneinfo/tab/zone_sun.tab"
 #endif
 
 typedef struct _TzDB TzDB;
diff '--exclude=.git' -urN a/panels/printers/Makefile.am b/panels/printers/Makefile.am
--- a/panels/printers/Makefile.am	2016-09-20 04:49:16.000000000 -0400
+++ b/panels/printers/Makefile.am	2016-09-28 00:08:29.101513819 -0400
@@ -10,6 +10,9 @@
 	-I$(top_srcdir)/shell/				\
 	-DGNOMELOCALEDIR="\"$(datadir)/locale\""	\
 	-DTEST_SRCDIR=\""$(srcdir)/"\"			\
+	-DREALDATADIR="\"$(datadir)\""                  \
+	-DPREFIX="\"$(prefix)\""                        \
+	-DLIBEXECDIR="\"$(libexecdir)\""                \
 	$(NULL)
 
 noinst_LTLIBRARIES = libprinters.la
diff '--exclude=.git' -urN a/panels/printers/cc-printers-panel.c b/panels/printers/cc-printers-panel.c
--- a/panels/printers/cc-printers-panel.c	2016-09-20 04:49:16.000000000 -0400
+++ b/panels/printers/cc-printers-panel.c	2016-09-28 00:06:38.848143847 -0400
@@ -2589,8 +2589,8 @@
 
   if (printer_name)
     {
-      const gchar  *const dirs[] = { "/usr/share/cups",
-                                     "/usr/local/share/cups",
+      const gchar  *const dirs[] = { REALDATADIR "/cups",
+                                     PREFIX "/local/share/cups",
                                      NULL };
       const gchar  *datadir = NULL;
       http_t       *http = NULL;
diff '--exclude=.git' -urN a/panels/printers/pp-host.c b/panels/printers/pp-host.c
--- a/panels/printers/pp-host.c	2016-04-26 07:03:05.000000000 -0400
+++ b/panels/printers/pp-host.c	2016-09-28 00:06:38.848143847 -0400
@@ -254,7 +254,7 @@
   data->devices->devices = NULL;
 
   argv = g_new0 (gchar *, 3);
-  argv[0] = g_strdup ("/usr/lib/cups/backend/snmp");
+  argv[0] = g_strdup (LIBEXECDIR "/cups/backend/snmp");
   argv[1] = g_strdup (priv->hostname);
 
   /* Use SNMP to get printer's informations */
